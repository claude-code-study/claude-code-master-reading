const members = require('./members.config');

/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  studySidebar: members.map((m) => ({
    type: 'category',
    label: `@${m.githubId}`,
    collapsible: true,
    collapsed: false,
    items: [{ type: 'autogenerated', dirName: m.dir }],
  })),
};

module.exports = sidebars;
